/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModelNoGraphicsComponent } from './model-no-graphics.component';

describe('Component: ModelNoGraphics', () => {
  it('should create an instance', () => {
    let component = new ModelNoGraphicsComponent();
    expect(component).toBeTruthy();
  });
});
