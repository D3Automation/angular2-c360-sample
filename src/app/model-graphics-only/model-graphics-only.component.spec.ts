/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModelGraphicsOnlyComponent } from './model-graphics-only.component';

describe('Component: ModelGraphicsOnly', () => {
  it('should create an instance', () => {
    let component = new ModelGraphicsOnlyComponent();
    expect(component).toBeTruthy();
  });
});
